sudo: required
language: c
services:
  - docker

script:
  - make bootstrap
  - make lint
  - make test
  - make build

deploy:
  provider: script
  script: bash scripts/install && bash scripts/deploy.sh
  on:
    branch: infra-testing
